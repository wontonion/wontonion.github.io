# Eslint 和 prettier

这两个工具都不是专门为了TS设计的，JS也可以使用。这里只是为了更加规范代码。
Eslint是一个静态语法分析器，旨在尽可能找出代码当中的逻辑和语法错误
`eslint.org`


prettier是一个代码格式化工具，旨在提供一致的代码风格
`prettier.io`

主要配置生成的是`.eslintrc.js`和`.prettierrc.js`, `.eslintignore`, `prettierignore`



